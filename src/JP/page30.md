-- left
[!](p30-spriteBot.png)
-- right
## スプライト
Mini Microには、*スプライト*と呼ばれる特別な描き方があります。スプライトとは、動いたり、回転したり、大きさや色を変えたりできる小さな絵のことで、しかもそれをとても速く行えます。
スプライトは、Pac-ManやSuper Mario Bros、Angry Birdsのようなコンピューターゲームで目にします。平たい小さな絵が画面の上を動きまわっていたら、それはほぼ間違いなくスプライトです。

-- left 65%
スプライトを準備する手順は、下のプログラムの2〜5行目に示されています。まず`new Sprite`でスプライトを作り、変数に代入します。次に、そのスプライトに表示させる画像を読み込みます。そして`display(4).sprites.push`で画面に追加して、見えるようにします。最後に、`x`（横）と`y`（縦）の位置を設定します。
-- right
[!](p30-ufoScreen.png)

-- full
[!](p30-listing1.png)

-- left
6〜10行目の`while`ループを使うと、スプライトを動かすことができます。ここでは、使う人から入力を受け取る新しい方法も登場します。`key.axis`です。この方法なら、矢印キーでもWASDキーでも、なんとゲームパッドでもスプライトを操作できます！
-- right
-- puzzle
UFOのスピードを2倍にしてみましょう！ヒント：今の速さは横方向が10、縦方向が6です。

-- pagebreak
-- left 30%
スプライトを作ったあとは、右の表にあるプロパティに値を代入することで、大きさ、色、回転、位置を変えられます。上のリストの7〜8行目や、下のリストの11〜12行目を見てみましょう。
-- right
-- table
スプライトのプロパティ
`spr.x` | スプライト_spr_の横の位置（0-960）
`spr.y` | スプライト_spr_の縦の位置（0-640）
`spr.rotation` | 回転の角度、度単位（0-360）
`spr.scale` | 大きさの倍率：1=そのまま、2=2倍、など
`spr.tint` | スプライトの色（color.white、color.red、など）
-- endtable

-- left
ふつうは、file.loadImageを直接使ってスプライトの画像を手に入れます。でも、ディスクにある画像ファイルが、実はフレームと呼ばれる小さな絵をたくさん集めたものになっていることもあります。それぞれのフレームは、アニメーションの1コマを表しています。
-- right
これを使うには、まず大きいほうの画像（この場合は*スプライトシート*と呼びます）を変数に読み込み、それに対して`getImage`を呼び出して個々のフレームを取り出します。下の例のとおりです。

-- full
[!](p30-listing2.png)
[!](p30-enemyFrames.png)

-- gap
